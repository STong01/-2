Java语言的特点：
1、面向对象：
    理解为把现实中的事物抽象成具有状态属性和行为方法的对象；
    三大特征：继承、多态、抽象
2、安全性：
    Java不支持指针
    Java的内部安全措施
3、平台无关性：
    编译后的字节码对应于Java虚拟机，因此Java虚拟机可以在任何平台上运行
4、支持多线程：
    Java是第一个在语言级提供内置多线支持的高级语言
5、内存管理
    Java对内存自动进行管理并进行垃圾回收